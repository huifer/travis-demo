
language: java

jdk:

  - openjdk8

services:
  - docker

cache:
  directories:
    - $HOME/.m2
after_success:
  - mvn clean test
  ## 生成测试报告
  - mvn jacoco:report
  ## 获取travis解密的token ,并调用coveralls-plugin上传报告
  - mvn coveralls:report -DrepoToken="y5kxbcxoRFn0DIefnIEjfftqg9tzGaEbF"